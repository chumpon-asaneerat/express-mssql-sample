/** The Nlib library. */
const nlib = {
    // common functions.
    /** The log function. */
    log: (message, ...optionalParams) => { console.log(message, ...optionalParams); }
}

/** The Nlib library. */
module.exports = exports = nlib;
