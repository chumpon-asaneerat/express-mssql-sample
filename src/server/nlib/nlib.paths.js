const path = require('path');
const rootPath = path.dirname(require.main.filename);

/** The Nlib Common path constants. */
const paths = {
    /** The workspace's root path. */
    root: rootPath
}

/** The Nlib Common path constants. */
module.exports = exports = paths;